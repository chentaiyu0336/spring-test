spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/rssystem?characterEncoding=utf-8&useSSL=false
    username: root
    password: pass
  jpa:
    hibernate:
      ddl-auto:  update # 如果数据库不存在就创建
    show-sql: true    # 打印数据库语句